[Unit]
Description=Transit Frontend
After=network.target
StartLimitIntervalSec=0

[Service]
Type=simple
Restart=on-failure
RestartSec=1
User=kyler
ExecStart=bash -c "source /home/kyler/.bash_profile; PORT=16172 bun run preview"
WorkingDirectory=/opt/catenary-frontend

[Install]
WantedBy=multi-user.target
